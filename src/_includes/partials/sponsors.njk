<section class="section sponsors">
  <span class="eyebrow center reveal">{{ idst.sponsors.eyebrow }}</span>
  <div class="sponsor-row reveal d1">
    {% for sponsor in idst.sponsors.items %}
    <span class="sponsor">{{ sponsor }}</span>
    {% endfor %}
  </div>
</section>
